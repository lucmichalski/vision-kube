apiVersion: v1
kind: Service
metadata:
  name: ltuengine-backend-ui
  namespace: kube-system
  labels:
    k8s-app: ltuengine-backend-ui
    kubernetes.io/cluster-service: "true"
    kubernetes.io/name: "LTU Engine v7.6 - Administration Panel"
spec:
  ports:
  - port: 8888
    protocol: TCP
    targetPort: ui
  selector:
    k8s-app: ltuengine-backend-ui
